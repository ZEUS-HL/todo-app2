# Base URL of the Express backend (no trailing slash).
# Leave empty when using Vercel serverless functions (/api/*).
# Set to http://localhost:3001 for local development with the Express server.
VITE_API_URL=http://localhost:3001
